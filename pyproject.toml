[project]
name = "synthetic-mindat-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.0.1",
]

[tool.uv.workspace]
members = [
    "bfcl_project/gorilla",
]
